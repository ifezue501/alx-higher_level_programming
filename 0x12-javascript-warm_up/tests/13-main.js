#!/usr/bin/node
// import the `add` function from the `13-add.js` file
const add = require('./13-add').add;
// call the `add` function with two arguments (3 and 5) and log the returned result to the console
console.log(add(3, 5));
