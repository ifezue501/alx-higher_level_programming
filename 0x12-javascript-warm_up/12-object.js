#!/usr/bin/node
// Define an object named myObject with properties type and value
const myObject = {
  type: 'object',
  value: 12
};
// Print the initial object to the console
console.log(myObject);
// Modify the value property of myObject
myObject.value = 89;
// Print the updated object to the console
console.log(myObject);
